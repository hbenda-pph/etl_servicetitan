# st2audio-job

Job de extracción: ServiceTitan Telecom API (GET /calls/{id}/recording) → Cloud Storage (`gs://{company_project}_audio/{year}/{month}/{lead_call_id}.mp3`) y generación de metadatos de auditoría.
